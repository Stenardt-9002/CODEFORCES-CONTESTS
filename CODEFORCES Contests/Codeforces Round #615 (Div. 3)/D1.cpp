#include <iostream>
#include <cstring>
#include <vector>
#include <algorithm>
#include <math.h>
using namespace std;
#define MAX_VAL 1000000000
typedef long long int lld;


int main(int argc, char const *argv[])
{

  lld queries,xval;
  lld *input,*output,*record;
  input = new lld[queries];
  record = new lld[queries];
  output = new lld[queries];

  for (lld i = 0; i < queries; i++)
  {


    cin>>input[i];

    if (i>0)
    {
      //begin move
      /* code */
    }



  }

  return 0;
}
